@model bool?
@{
    var descriptor = ViewData.ModelMetadata.GetViewDescriptor();
    descriptor.Classes.Remove("form-control");
}
<div class="checkbox">
    <label>
        @Html.CheckBox("", Model, descriptor?.ToHtmlProperties())
    </label>    
</div>
